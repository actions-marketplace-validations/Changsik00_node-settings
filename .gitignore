node_modules
dist
coverage
.nyc_output
.doc-check-tmp
.tmp-ws-e2e-*
.stryker-tmp
reports
*.tsbuildinfo
.DS_Store
.env
.env.*
!.env.example
!.env.*.example
!.env.*.sample
!.env.sample
# The canonical worked example deliberately commits its env files so
# users can read them directly. Exempt sample/'s .env files from the
# catch-all (the sample's own .gitignore is illustrative — it shows
# what your *real* project's .gitignore should look like).
!sample/.env
!sample/.env.*
*.log
logs
.idea
.vscode
.turbo
.claude
