{
  "name": "@env-kit/consumer-smoke-test",
  "private": true,
  "version": "0.0.0",
  "description": "External-consumer smoke test for @env-kit/node-settings. Installs the published package from npm and exercises the public API with strict tsc + skipLibCheck:false. CI-only; never published.",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "smoke": "tsx app.ts",
    "all": "pnpm check && pnpm smoke"
  },
  "dependencies": {
    "@env-kit/node-settings": "^0.11.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
