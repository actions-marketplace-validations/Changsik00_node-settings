# .env.stage — staging / pre-prod environment.
# Same pattern as .env.dev — committed non-secrets, CI fills the rest.

APP_ENV=stage
DB_HOST=db.stage.internal
DB_PASSWORD=REPLACE_VIA_CI
REDIS_URL=redis://redis.stage.internal:6379
