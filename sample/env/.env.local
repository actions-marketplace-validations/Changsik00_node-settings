# .env.local — local development on a contributor's machine.
# Wins over .env and .env.<mode> (typically gitignored in real projects;
# committed here for demonstration).

APP_ENV=local
DB_HOST=localhost
DB_PASSWORD=local-dev-password
REDIS_URL=redis://localhost:6379
